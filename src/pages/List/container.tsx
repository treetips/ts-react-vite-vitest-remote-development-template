import { Layout } from "../../components/layout/Layout/container";
import { ListPagePresenter } from "./presenter";

export const ListPage: React.FC = () => {
  return (
    <Layout>
      <ListPagePresenter />
    </Layout>
  );
};
